# 2026-09-23 生产 QA 与代码审查

## 范围与方法

- 以 [`func.md`](func.md) §20、[`frontend.md`](frontend.md) §7、[`harness.md`](harness.md) §14 为验收依据；目标状态与已实现能力按 [`README.md`](README.md) 区分。
- 使用内置浏览器访问生产站点，以已有登录会话执行操作；不在报告中记录账号、密钥或会话令牌。
- 自动化验证在本地隔离环境执行，不把它等同于生产浏览器全链路测试。

## 生产浏览器实测

| 功能 | 结果 |
|---|---|
| 今日页 | 旧 PWA 首次显示 0/3；接受新版 PWA 后在线读取显示 0/0 及“今天还没有计划”，因此先前 0/3 不能作为当前计划的验收证据。未重生成或更改真实账号计划。 |
| 目标树 | 创建 `QA 验证目标 2026-09-23`，添加 `QA 验证任务`，刷新后确认持久化；最终版本又创建 `QA 最终列表刷新验证` 及 `QA 修订号验证任务`，新目标立即进入列表，任务树立即从 Revision 0 变成 1，空白字段没有误报。在该 QA 目标上确认应用 Agent 四节点拆解提案，Revision 升至 2。新 PWA 再次进入目标树稳定显示 Revision 2；断网进入目标树仍能显示用户隔离的离线快照。 |
| 地图 | 拖动 QA 节点“记录不同步问题并复测”，提示坐标保存为 (31, 86)；切页后重新进入地图，节点仍在新位置。 |
| 对话 | 新建会话，发送只读 QA 指令，收到“连接成功”；思考过程折叠入口可见，重新切页后消息仍在。 |
| 作业队列 | 实时状态连接正常，成功作业可查询，活跃/排队/运行中/失败筛选可见。两次 QA 修订提案作业均完成三次尝试后报 `PROVIDER_ERROR`，错误详情为本机模型代理请求等待响应头超时；未生成可供确认/拒绝的修订提案。 |
| 周复盘 | 当前周及上一周可切换，空周显示明确的无有效专注时间状态。 |
| 设备与后台 | 设备创建入口、用户管理、模型管理表单可访问；审计日志显示既有用户创建事件。当前无数据库 Provider，密钥未回显。 |

本轮没有通过浏览器执行重规划、今日计划项完成、专注记录、修订提案确认/拒绝、删除、设备 Token 生成、管理员用户/密钥变更、语音录制和图片上传。这些操作可能改变真实业务记录、创建持久凭据或涉及浏览器权限，**不能宣称已经完成生产全操作 QA**。自动化测试覆盖其中的接口和业务不变量，但不能替代生产实机复核。QA 创建的三个目标、手工任务、Agent 四节点拆解及会话保留在生产数据中，供复核；不操作真实业务目标。

## 审查发现与修复

1. 新建目标成功后仍显示旧目标详情：`Goals.createGoal` 未选中新目标；即使选中，PWA 的 `/goals` stale-while-revalidate 快照仍会短暂覆盖新列表。任务树的 Revision 也可能在写入后暂时过期。现在使用创建响应选中，并在目标/任务树变更后以唯一查询参数读取最新列表和树，保留常规请求的离线快照路径。生产浏览器中两层问题均已复现。
2. 手工添加任务成功后，清空的必填控件出现英文原生“Please fill out this field”提示。改为已有的禁用按钮和提交函数校验，避免成功态显示错误。
3. macOS 默认临时目录过长，三个 Unix socket 测试因路径长度失败。测试在 macOS 使用短临时路径。
4. HTTP Agent 测试取消 Worker 后未等待 goroutine 退出，偶发数据库临时目录清理竞态。测试在关闭数据库之前等待 Worker 停止。
5. 文档索引把 Schema 版本写成 5，而仓库已有迁移 8；已校正。
6. 再次进入目标树时，`StaleWhileRevalidate` 返回旧 `/goals` 快照后虽在后台刷新缓存，已挂载的 React 视图不会再次读取，导致刚创建的目标暂时消失、默认选中另一个目标。快照路由改为 `NetworkFirst`，保留用户隔离与离线回退；新增产物策略回归断言。新版 PWA 上在线再次进入、离线回退均通过浏览器复核。
7. 两次修订任务树作业均因本机模型代理超时而失败，队列正确显示失败及重试详情。该外部依赖故障阻断了修订提案确认/拒绝的生产浏览器 QA，尚未归因为 FastTask 代码问题。

## 验证命令

- `go test ./...`、`go vet ./...`：通过；新增带刷新查询参数的目标列表和任务树 HTTP 契约测试。
- `cd web && npm run build && npm test`：本地构建通过，24 个测试文件、166 个测试通过；远端暂存构建和同一测试集通过。
- 前端产物测试依赖先构建 `web/dist`，不能在全新安装依赖后直接运行 `npm test` 来验证产物。

## 部署

生产前端已从 `402fc34` 更新至 `e47ab9e`；本轮只有前端缓存策略与文档变化，未重启后端。已有数据库快照 `backups/predeploy-20260923-qa.db`、早期前端产物备份 `backups/web-dist-predeploy-20260923`、`backups/web-dist-d15de64`；此次切换前产物另存为 `backups/web-dist-e47ab9e-before`。远端 `web/dist.new` 构建及 166 个前端测试通过后才切换。实际监听地址的 readiness、首页、`sw.js` 和 WASM 均为 HTTP 200；内置浏览器接受 PWA 更新后复测目标树在线新鲜读取、地图坐标持久化和离线快照回退通过。后续纯文档提交无需重新构建前端。
